<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">

        <item android:title="@string/myMangaFragment"
            android:id="@+id/menu_mylib"
            android:icon="@drawable/ic_action_book"/>

        <item android:title="@string/myMangaList"
            android:id="@+id/menu_mylists"
            android:icon="@drawable/ic_list"
            android:checked="true"/>

        <item android:title="@string/myDownloads"
            android:id="@+id/menu_mydownloads"
            android:icon="@drawable/ic_action_download"/>

    </group>

    <item android:title="Help">
        <menu>
            <item android:title="@string/action_settings"
                android:id="@+id/settings"
                android:icon="@drawable/ic_settings"/>
        </menu>
    </item>

</menu>